query fetchEvents($eventsRoute: Boolean!) {
  events(sortBy: CREATEDAT_DESC) {
    _id
    eventTitle
    eventDescription @include(if: $eventsRoute)
    cheapestTicket {
      ticketPrice
    }
    isProvisions
    eventDate
    eventImage
    color
    textColor
    soldOut
  }
}
